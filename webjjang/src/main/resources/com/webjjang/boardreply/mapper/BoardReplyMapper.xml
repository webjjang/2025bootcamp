<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "file:///c:/dtd/mybatis-3-mapper.dtd">
<!-- "http://mybatis.org/dtd/mybatis-3-mapper.dtd" -->

<mapper namespace="com.webjjang.boardreply.mapper.BoardReplyMapper">

	<!-- 1-1. list -->
	<select id="list" resultType="com.webjjang.boardreply.vo.BoardReplyVO">
		select rno, no, content, id, name, writeDate
		from(
			select rownum rnum, rno, no, content, id, name, writeDate
			from(
				select b.rno, b.no, b.content, b.id, m.name, b.writeDate
				from board_reply b, member m
				where (b.no = #{no}) and (b.id = m.id)
				order by b.rno desc
			)
		) where rnum between #{startRow} and #{endRow}
	</select>
	
	<!-- 1-2. getTotalRow -->
	<select id="getTotalRow" resultType="Long">
		select count(*) 
		from board_reply
		where no = #{no}
	</select>
	
	<!-- 2. write -->
	<insert id="write">
		insert into board_reply(rno, no, content, id)
		values(board_reply_seq.nextval, #{no}, #{content}, #{id})
	</insert>
	
	<!-- 3. update -->
	<update id="update">
		update board_reply
		set content = #{content}
		where rno = #{rno} and id = #{id}
	</update>
	
	<!-- 4. delete -->
</mapper>